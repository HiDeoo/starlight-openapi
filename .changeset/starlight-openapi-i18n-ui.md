---
"starlight-openapi": minor
---

Add internationalization (i18n) support for Starlight OpenAPI UI strings.

This change makes user-facing labels translatable via Starlight UI translations (e.g. `src/content/i18n/{locale}.json`),
including headings, sidebar labels, and other UI text used by the generated OpenAPI pages.


