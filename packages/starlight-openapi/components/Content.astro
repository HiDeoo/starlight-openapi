---
import { getContentEntries, type Content } from '../libs/content'

import MediaEntries from './MediaEntries.astro'

interface Props {
  content: Content
}

const { content } = Astro.props

const entries = getContentEntries(content)
---

<MediaEntries {entries} />
