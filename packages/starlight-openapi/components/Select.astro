---
import { Icon } from '@astrojs/starlight/components'

interface Props {
  label: string
  options: string[] | undefined
}

const { label, options } = Astro.props
---

{
  options && options.length > 0 && (
    <label class="not-content sl-openapi-select">
      <span class="sr-only">{label}</span>
      <select>
        {options.map((option) => (
          <option value={option}>{option}</option>
        ))}
      </select>
      <Icon name="down-caret" class="sl-openapi-select-caret" />
    </label>
  )
}

<style>
  .sl-openapi-select {
    color: var(--sl-color-gray-2);
    display: flex;
    margin-bottom: 0.5rem;
    position: relative;
  }

  .sl-openapi-select:hover {
    color: var(--sl-color-gray-1);
  }

  .sl-openapi-select select {
    appearance: none;
    background-color: var(--sl-color-gray-5);
    border: 1px solid var(--sl-color-gray-4);
    color: inherit;
    padding-block: 0.375rem;
    padding-inline: 0.5rem 2rem;
    text-overflow: ellipsis;
    width: 100%;
  }

  .sl-openapi-select select:hover {
    border-color: var(--sl-color-gray-2);
  }

  .sl-openapi-select-caret {
    font-size: 1.25rem;
    font-size: 1.375rem;
    inset-inline-end: 0.8rem;
    pointer-events: none;
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
  }
</style>
