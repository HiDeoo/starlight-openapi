---
import Tag from './Tag.astro'

interface Props {
  tags: unknown[]
}

const { tags: allTags } = Astro.props

const tags = allTags.filter((tag) => typeof tag === 'string')
---

{
  tags.length > 0 && (
    <div class="tags">
      {tags.map((tag) => (
        <Tag>{tag}</Tag>
      ))}
    </div>
  )
}

<style>
  .tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.4rem;
    margin-block: 0.5rem;
  }
</style>
