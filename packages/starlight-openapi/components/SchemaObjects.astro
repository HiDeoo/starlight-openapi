---
import type { SchemaObjects } from '../libs/schemaObject'

import SchemaObject from './SchemaObject.astro'

interface Props {
  schemaObjects: SchemaObjects
}

const {
  schemaObjects: { schemaObjects, type },
} = Astro.props
---

<div>MULTIPLE TYPE: {type}</div>
{schemaObjects.map((schemaObject) => <SchemaObject schemaObject={schemaObject} />)}
